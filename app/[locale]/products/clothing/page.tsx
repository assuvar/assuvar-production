'use client';

import ProductTeaser from '@/components/products/ProductTeaser';

export default function ClothingPage() {
    return (
        <ProductTeaser
            title="Assuvar for Clothing"
            description="The dedicated operating system for fashion retail. Managing sizes, styles, and seasons has never been easier."
        />
    );
}
